package com.transformersas.marketplace.stock;

/** ENTRY: llegó mercancía y se suma al stock. ADJUSTMENT: el vendedor fija el conteo real (por un inventario físico). */
public enum StockMovementType {
    ENTRY,
    ADJUSTMENT
}
