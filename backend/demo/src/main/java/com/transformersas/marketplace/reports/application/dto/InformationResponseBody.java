package com.transformersas.marketplace.reports.application.dto;

/** Respuesta del reportante a una solicitud de información del agente (RF-148). */
public record InformationResponseBody(String text) {
}
