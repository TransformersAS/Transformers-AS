package com.transformersas.marketplace.reports.domain.model;

/** Estado de visibilidad de un contenido tras la moderación. */
public enum ContentModerationState {
    VISIBLE,
    OCULTO_TEMPORAL,
    RETIRADO
}
